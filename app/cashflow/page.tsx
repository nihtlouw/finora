import { redirect } from 'next/navigation'
import FinoraShell from '@/components/finora-shell'
import { PageHeader, Card, StatCard, Badge, money } from '@/components/finora-ui'
import { getCurrentFinoraContext } from '@/lib/auth/current-user'
import { prisma } from '@/lib/db/prisma'
export const dynamic='force-dynamic'
export default async function CashflowPage(){const c=await getCurrentFinoraContext();if(!c)redirect('/sign-in');const rows=await prisma.cashflowTransaction.findMany({where:{OR:[{workspaceId:c.workspace.id},{payment:{invoice:{client:{workspaceId:c.workspace.id}}}},{expense:{vendor:{workspaceId:c.workspace.id}}}]},orderBy:{transactionDate:'desc'},take:100});const income=rows.filter(x=>x.type==='INCOME').reduce((s,x)=>s+Number(x.amount),0);const expense=rows.filter(x=>x.type==='EXPENSE').reduce((s,x)=>s+Number(x.amount),0);return <FinoraShell workspaceName={c.workspace.name} role={c.user.role} title="Cash Flow"><div className="f-content"><PageHeader eyebrow="Kas" title="Cash Flow" description="Pantau uang masuk, keluar, dan saldo pergerakan kas dari transaksi nyata."/><div className="f-grid-3"><StatCard label="Pemasukan" value={money(income)} trend="Payment" icon="↗"/><StatCard label="Pengeluaran" value={money(expense)} trend="Expense" icon="↘"/><StatCard label="Net cashflow" value={money(income-expense)} trend="Income - Expense" icon="◎"/></div><div style={{height:16}}/><Card><div className="f-card-head"><div><h3>Transaksi</h3><p>Data dari Payment dan Expense yang sudah tercatat.</p></div></div><div style={{overflowX:'auto'}}><table className="f-table"><thead><tr><th>Tanggal</th><th>Kategori</th><th>Sumber</th><th>Tipe</th><th>Nominal</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{new Date(x.transactionDate).toLocaleDateString('id-ID')}</td><td>{x.category}</td><td>{x.sourceRef||'—'}</td><td><Badge tone={x.type==='INCOME'?'green':'red'}>{x.type}</Badge></td><td className="f-number">{x.type==='INCOME'?'+':'-'}{money(Number(x.amount))}</td></tr>)}</tbody></table></div>{!rows.length&&<div className="f-empty"><strong>Belum ada transaksi</strong>Transaksi akan otomatis muncul saat payment atau expense tercatat.</div>}</Card></div></FinoraShell>}

import { NextResponse } from 'next/server'
import { prisma } from '@/lib/db/prisma'
import { getCurrentFinoraContext } from '@/lib/auth/current-user'
export const dynamic='force-dynamic'
export async function GET(){const c=await getCurrentFinoraContext();if(!c)return NextResponse.json({error:'Unauthenticated'},{status:401});const rows=await prisma.invoice.findMany({where:{client:{workspaceId:c.workspace.id}},include:{client:true,items:true,payments:true},orderBy:{dueDate:'asc'}});return NextResponse.json({invoices:rows})}
export async function POST(req:Request){const c=await getCurrentFinoraContext();if(!c)return NextResponse.json({error:'Unauthenticated'},{status:401});if(!['OWNER','FINANCE','SALES'].includes(c.user.role))return NextResponse.json({error:'Tidak memiliki akses.'},{status:403});try{const b=await req.json();const client=await prisma.clientVendor.findFirst({where:{id:b.clientId,workspaceId:c.workspace.id,type:'CLIENT'}});if(!client)return NextResponse.json({error:'Klien tidak ditemukan.'},{status:404});const items=Array.isArray(b.items)?b.items:[];const total=items.reduce((s:any,x:any)=>s+Number(x.qty||0)*Number(x.unitPrice||0),0);const row=await prisma.invoice.create({data:{clientId:client.id,invoiceNumber:b.invoiceNumber||`INV-${new Date().getFullYear()}-${Date.now().toString().slice(-6)}`,status:'UNPAID',dueDate:new Date(`${b.dueDate}T00:00:00Z`),totalAmount:total,items:{create:items.map((x:any)=>({description:String(x.description),qty:Number(x.qty),unitPrice:Number(x.unitPrice)}))}},include:{client:true,items:true,payments:true}});return NextResponse.json({invoice:row},{status:201})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Gagal membuat invoice.'},{status:400})}}

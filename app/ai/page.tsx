import { redirect } from 'next/navigation'
import FinoraShell from '@/components/finora-shell'
import { PageHeader,Card,Badge } from '@/components/finora-ui'
import { getCurrentFinoraContext } from '@/lib/auth/current-user'
export default async function AIPage(){const c=await getCurrentFinoraContext();if(!c)redirect('/sign-in');return <FinoraShell workspaceName={c.workspace.name} role={c.user.role} title="AI Assistant"><div className="f-content"><PageHeader eyebrow="Intelligence" title="AI Assistant" description="Tanya data keuangan dengan bahasa sehari-hari. Integrasi model AI akan diaktifkan setelah core finance stabil."/><Card><div className="f-card-head"><div><h3>Preview pengalaman</h3><p>Contoh pertanyaan yang akan didukung.</p></div><Badge tone="amber">Integration pending</Badge></div><div className="f-grid-3" style={{padding:18}}>{['Berapa margin bulan lalu?','Invoice mana yang paling perlu ditagih?','Kategori biaya apa yang paling besar?'].map(q=><div className="f-card pad" key={q}><strong>{q}</strong><p className="f-muted">Akan dijawab dari data workspace setelah AI provider terhubung.</p><span className="f-badge neutral">Contoh</span></div>)}</div></Card></div></FinoraShell>}
